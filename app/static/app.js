const $=s=>document.querySelector(s);let token=localStorage.getItem('hf_token'),me=JSON.parse(localStorage.getItem('hf_me')||'null');
const esc=v=>String(v??'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));
async function api(url,opt={}){opt.headers={...(opt.headers||{}),...(token?{Authorization:'Bearer '+token}:{})};if(opt.body&&typeof opt.body==='object'&&!opt.body.append){opt.headers['Content-Type']='application/json';opt.body=JSON.stringify(opt.body)}let r=await fetch('/api'+url,opt),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.detail||'Request failed');return d}
function shell(content){if(me?.role==='candidate')content='<div class="notice"><b>Fair hiring:</b> An AI tool may summarise your CV for the recruiter. It does not score, rank, or decide anything. People make every decision.</div>'+content;$('#app').innerHTML=`<div class="shell"><div class="top"><div class="brand">✦ Nowshera HireOS <small class="brand-mark">/ HUMAN-LED</small></div>${me?`<div class="small">${esc(me.name)} · ${esc(me.role)} <button class="btn alt" onclick="logout()">Sign out</button></div>`:''}</div>${content}</div>`}
function logout(){localStorage.clear();location.reload()}
function auth(){shell(`<div class="hero"><div class="hero-copy"><span class="eyebrow">NOWSHERA DIGITAL · HIRING OS</span><h1>Find the right people.<br><em>Build what matters.</em></h1><p class="hero-sub">A calm, intelligent hiring workspace where technology helps and people decide.</p><div class="hero-tags"><span>✦ Fair by design</span><span>◈ Human decisions</span><span>⌁ One clear pipeline</span></div></div><div class="hero-3d" id="hero3d"><div class="orb orb-a"></div><div class="orb orb-b"></div><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><div class="float-card card-candidate"><span class="mini-icon">✦</span><div><b>Candidate</b><small>CV received</small></div><strong>✓</strong></div><div class="float-card card-ai"><span class="mini-icon">◈</span><div><b>AI co-pilot</b><small>Human-reviewed</small></div><strong>98%</strong></div><div class="float-card card-stage"><span class="mini-icon">→</span><div><b>Next step</b><small>Interview ready</small></div><strong>10:00</strong></div><div class="core-logo">N<span>H</span></div></div></div><div class="auth-grid"><form class="card auth-card" onsubmit="signin(event)"><div class="form-kicker">WELCOME BACK</div><h2>Sign in</h2><p class="muted">Continue to your Nowshera HireOS workspace.</p><input class="input" name="email" type="email" placeholder="Email" required><input class="input" name="password" type="password" placeholder="Password" required><button class="btn btn-glow">Enter workspace <span>→</span></button><p class="muted small">Use your invited Admin or Recruiter account, or your candidate account.</p></form><form class="card auth-card" onsubmit="signup(event)"><div class="form-kicker">NEW HERE?</div><h2>Join as a candidate</h2><p class="muted">Create your account in under a minute.</p><input class="input" name="name" placeholder="Full name" required><input class="input" name="phone" placeholder="Phone"><input class="input" name="email" type="email" placeholder="Email" required><input class="input" name="password" type="password" placeholder="Password (6+ chars)" required><button class="btn btn-dark">Create account <span>→</span></button></form></div>`);init3D()}
function init3D(){const el=$('#hero3d');if(!el)return;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--rx',`${-y*10}deg`);el.style.setProperty('--ry',`${x*12}deg`)});el.addEventListener('pointerleave',()=>{el.style.setProperty('--rx','0deg');el.style.setProperty('--ry','0deg')})}
async function signin(e){e.preventDefault();try{save(await api('/auth/login',{method:'POST',body:Object.fromEntries(new FormData(e.target))}))}catch(x){alert(x.message)}}
async function signup(e){e.preventDefault();try{const d=await api('/auth/signup',{method:'POST',body:Object.fromEntries(new FormData(e.target))});if(d.detail&&!d.token)return alert(d.detail);save(d)}catch(x){alert(x.message)}}
function save(d){token=d.token;me=d.user;localStorage.setItem('hf_token',token);localStorage.setItem('hf_me',JSON.stringify(me));render()}
function stepper(hasCV){return `<div class="stepper"><button class="step done" onclick="jumpTo('cv-section')"><b>1</b><span>Account</span></button><button class="step ${hasCV?'done':''}" onclick="jumpTo('cv-section')"><b>2</b><span>Upload CV</span></button><button class="step" onclick="jumpTo('jobs-section')"><b>3</b><span>Apply</span></button><button class="step" onclick="jumpTo('applications-section')"><b>4</b><span>Track</span></button></div>`}
function jumpTo(id){document.getElementById(id)?.scrollIntoView({behavior:'smooth',block:'start'})}
async function candidate(){let jobs=await api('/jobs'),apps=await api('/candidate/applications');let hasCV=apps.length>0;shell(`<div class="page-heading"><div><span class="eyebrow">CANDIDATE PORTAL</span><h1>Find your next move.</h1><p class="muted">Everything you need from first application to final decision.</p></div></div>${stepper(hasCV)}<section id="cv-section" class="card cv-card"><div><span class="form-kicker">STEP 2 · YOUR CV</span><h2>Upload your CV</h2><p class="muted">PDF only, maximum 2 MB. You can replace it later; applications already sent keep the old CV.</p></div><div class="upload-row"><input id="cv" type="file" accept="application/pdf"><button class="btn" onclick="uploadCV()">Upload PDF <span>↑</span></button></div></section><div id="jobs-section" class="section-title"><div><span class="form-kicker">STEP 3 · OPPORTUNITIES</span><h2>Open jobs</h2></div><span class="pill">${jobs.length} available</span></div><div class="job-grid">${jobs.map(j=>`<article class="job-card"><div class="job-top"><span class="pill">${esc(j.job_type)}</span><span class="deadline">Apply by ${esc(j.deadline)}</span></div><h2>${esc(j.title)}</h2><p class="muted">${esc(j.department)} · ${esc(j.location)}</p><p class="job-description">${esc(j.description||'Join our team and make an impact.')}</p><button class="btn" onclick="applyJob('${esc(j.id)}')">Apply with my CV <span>→</span></button></article>`).join('')||'<div class="empty card">No open jobs right now. Check back soon.</div>'}</div><div id="applications-section" class="section-title"><div><span class="form-kicker">STEP 4 · YOUR JOURNEY</span><h2>My applications</h2></div></div><div class="applications-list">${apps.map(applicationCard).join('')||'<div class="empty card">Your applications will appear here after you apply.</div>'}</div>`)}
function applicationCard(a){let final=['Hired','Rejected','Withdrawn'].includes(a.stage);return `<article class="application-card"><div class="application-main"><div class="app-icon">${a.stage==='Hired'?'✓':a.stage==='Rejected'?'×':'→'}</div><div><h3>${esc(a.title)}</h3><p class="muted small">${esc(a.department||'')} · Applied ${esc((a.created_at||'').slice(0,10))}</p></div><span class="stage stage-${esc(a.stage)}">${esc(a.stage)}</span></div><div class="timeline"><span class="active">Applied</span><i></i><span class=${['Shortlisted','Interview','Offer','Hired'].includes(a.stage)?'active':''}>Shortlisted</span><i></i><span class=${['Interview','Offer','Hired'].includes(a.stage)?'active':''}>Interview</span><i></i><span class=${['Offer','Hired'].includes(a.stage)?'active':''}>Offer</span><i></i><span class=${a.stage==='Hired'?'active':''}>Decision</span></div>${a.starts_at?`<div class="interview-box"><b>Interview scheduled</b><span>${esc(a.starts_at)} · ${esc(a.interview_location||a.meeting_link||'Details shared by recruiter')}</span></div>`:''}<div class="app-actions">${final?'':`<button class="btn danger" onclick="withdraw('${esc(a.id)}')">Withdraw application</button>`}</div></article>`}
async function uploadCV(){let f=$('#cv')?.files[0],fd=new FormData();if(!f)return alert('Choose a PDF CV first');if(f.type!=='application/pdf')return alert('CV must be a PDF file');if(f.size>2*1024*1024)return alert('CV must be 2 MB or smaller');fd.append('file',f);try{await api('/candidate/cv',{method:'POST',body:fd});alert('CV uploaded successfully');candidate()}catch(x){alert(x.message)}}
async function applyJob(id){if(!confirm('Apply with your current PDF CV? An AI summary may be prepared for the recruiter, but people make every decision.'))return;try{await api('/applications/'+id,{method:'POST'});alert('Application received. We will email you with updates.');candidate()}catch(x){alert(x.message)}}
async function withdraw(id){if(!confirm('Withdraw this application? You can apply again later with a new CV if the job is still open.'))return;try{await api('/applications/'+id+'/withdraw',{method:'POST'});candidate()}catch(x){alert(x.message)}}
async function recruiter(){let apps=await api('/recruiter/applications');shell(`<div class="page-heading"><div><span class="eyebrow">RECRUITER PORTAL</span><h1>Your hiring desk.</h1><p class="muted">Only jobs assigned to you appear here. Review the CV, read the AI helper, then make the human decision.</p></div></div><div class="recruiter-stats"><div class="stat-card"><b>${apps.length}</b><span>Applicants</span></div><div class="stat-card"><b>${apps.filter(a=>a.stage==='Shortlisted').length}</b><span>Shortlisted</span></div><div class="stat-card"><b>${apps.filter(a=>a.stage==='Interview').length}</b><span>Interviews</span></div></div><div class="grid">${apps.map(a=>`<div class="card recruiter-card"><div class="app-header"><span class="stage stage-${esc(a.stage)}">${esc(a.stage)}</span><span class="muted small">Updated ${esc((a.updated_at||'').slice(0,10))}</span></div><h2>${esc(a.title)}</h2><p><b>${esc(a.candidate_name)}</b> · ${esc(a.candidate_email)}</p><div class="review-actions"><button class="btn alt" onclick="openCV('${esc(a.id)}')">Open CV ↗</button><button class="btn alt" onclick="retrySummary('${esc(a.id)}')">Try again</button></div><div class="ai-box"><b>◈ AI summary <small>Human review required</small></b><p>${esc(a.ai_summary||'Summary not available. Press Try again to regenerate it.')}</p></div><label>Private recruiter note</label><textarea id="n-${esc(a.id)}" placeholder="Only recruiters and admins can see this">${esc(a.note||'')}</textarea><button class="btn alt" onclick="note('${esc(a.id)}')">Save private note</button><div class="stage-controls"><select id="s-${esc(a.id)}"><option>Applied</option><option>Shortlisted</option><option>Offer</option><option>Hired</option><option>Rejected</option></select><button class="btn" onclick="stage('${esc(a.id)}')">Save stage</button></div>${a.stage==='Shortlisted'?`<button class="btn schedule-btn" onclick="schedule('${esc(a.id)}')">Schedule 1-hour interview</button>`:''}</div>`).join('')||'<div class="empty card">No applicants in your assigned jobs.</div>'}</div>`)}
async function note(id){try{await api('/applications/'+id+'/note',{method:'POST',body:{note:$('#n-'+id).value}});alert('Private note saved')}catch(x){alert(x.message)}}async function retrySummary(id){try{await api('/applications/'+id+'/summary/retry',{method:'POST'});alert('AI summary regenerated');recruiter()}catch(x){alert(x.message)}}async function openCV(id){try{let r=await fetch('/api/applications/'+id+'/cv',{headers:{Authorization:'Bearer '+token}});if(!r.ok)throw Error('CV could not be opened');let b=await r.blob();window.open(URL.createObjectURL(b),'_blank')}catch(x){alert(x.message)}}async function stage(id){try{await api('/applications/'+id+'/stage',{method:'POST',body:{stage:$('#s-'+id).value}});recruiter()}catch(x){alert(x.message)}}async function schedule(id){let start=prompt('Start time ISO, e.g. 2026-10-01T10:00:00');if(!start)return;let place=prompt('Location or meeting link');if(!place)return;try{await api('/applications/'+id+'/interview',{method:'POST',body:{starts_at:start,location:place}});alert('1-hour interview scheduled; candidate notification queued');recruiter()}catch(x){alert(x.message)}}
async function admin(){let d=await api('/admin/dashboard'),rs=await api('/admin/recruiters');let count=(job,stage)=>d.counts.filter(x=>x.job_id===job&&x.stage===stage)[0]?.total||0;shell(`<div class="page-heading"><div><span class="eyebrow">ADMIN CONTROL ROOM</span><h1>Hiring, at a glance.</h1><p class="muted">Create jobs, assign recruiters and see every stage in one place.</p></div></div><div class="admin-grid">${d.jobs.map(j=>`<div class="card admin-job"><div class="app-header"><span class="pill">${esc(j.status)}</span><span class="muted small">${esc(j.openings)} openings</span></div><h2>${esc(j.title)}</h2><div class="count-row"><span>Applied <b>${count(j.id,'Applied')}</b></span><span>Shortlisted <b>${count(j.id,'Shortlisted')}</b></span><span>Interview <b>${count(j.id,'Interview')}</b></span><span>Offer <b>${count(j.id,'Offer')}</b></span><span>Hired <b>${count(j.id,'Hired')}</b></span><span>Rejected <b>${count(j.id,'Rejected')}</b></span><span>Withdrawn <b>${count(j.id,'Withdrawn')}</b></span></div><div class="admin-actions"><button class="btn alt" onclick="jobStatus('${esc(j.id)}','${esc(j.status)}')">${j.status==='open'?'Close job':'Open job'}</button><select id="r-${esc(j.id)}">${rs.filter(r=>r.active).map(r=>`<option value="${esc(r.id)}">${esc(r.name)}</option>`).join('')}</select><button class="btn" onclick="assignRecruiter('${esc(j.id)}')">Assign recruiter</button></div></div>`).join('')||'<p>No jobs yet.</p>'}</div><div class="admin-two-col"><div class="card"><span class="form-kicker">JOB SETUP</span><h2>Create draft job</h2><form onsubmit="createJob(event)" class="grid"><input class="input" name="title" placeholder="Title" required><input class="input" name="department" placeholder="Department" required><input class="input" name="location" placeholder="Location" required><select name="job_type"><option>Full-time</option><option>Part-time</option><option>Internship</option></select><input class="input" name="deadline" type="date" required><input class="input" name="openings" type="number" min="1" value="1"><textarea name="description" placeholder="Description"></textarea><textarea name="requirements" placeholder="Requirements"></textarea><button class="btn">Save as Draft</button></form></div><div class="card"><span class="form-kicker">TEAM ACCESS</span><h2>Recruiters</h2><form onsubmit="addRecruiter(event)"><input class="input" name="name" placeholder="Recruiter name" required><input class="input" name="email" type="email" placeholder="Recruiter email" required><input class="input" name="phone" placeholder="Phone"><button class="btn">Add recruiter</button></form><div class="recruiter-list">${rs.map(r=>`<div class="row"><span><b>${esc(r.name)}</b><small class="muted">${esc(r.email)}</small></span>${r.active?`<button class="btn danger" onclick="deactivateRecruiter('${esc(r.id)}')">Deactivate</button>`:'<span class="pill">Inactive</span>'}</div>`).join('')||'<p class="muted">No recruiters added.</p>'}</div></div></div>`)}
async function createJob(e){e.preventDefault();try{await api('/admin/jobs',{method:'POST',body:Object.fromEntries(new FormData(e.target))});admin()}catch(x){alert(x.message)}}async function addRecruiter(e){e.preventDefault();try{await api('/admin/recruiters',{method:'POST',body:Object.fromEntries(new FormData(e.target))});alert('Recruiter created; password setup email is ready for your email provider');admin()}catch(x){alert(x.message)}}async function deactivateRecruiter(id){if(!confirm('Deactivate this recruiter?'))return;try{await api('/admin/recruiters/'+id+'/deactivate',{method:'POST'});admin()}catch(x){alert(x.message)}}async function assignRecruiter(job){try{await api('/admin/jobs/'+job+'/assign',{method:'POST',body:{recruiter_id:$('#r-'+job).value}});alert('Recruiter assigned');admin()}catch(x){alert(x.message)}}async function jobStatus(id,status){try{await api('/admin/jobs/'+id+'/'+(status==='open'?'close':'open'),{method:'POST'});admin()}catch(x){alert(x.message)}}
async function render(){if(!me)return auth();if(me.role==='candidate')candidate();else if(me.role==='recruiter')recruiter();else admin()}render();

